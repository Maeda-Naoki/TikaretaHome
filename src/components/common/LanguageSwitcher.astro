---
const locale = Astro.currentLocale ?? 'ja';
const currentPath = Astro.url.pathname;

// Generate alternate URL
let alternateUrl: string;
if (locale === 'ja') {
  // Switch to English
  alternateUrl = `/en${currentPath}`;
} else {
  // Switch to Japanese (remove /en prefix)
  alternateUrl = currentPath.replace(/^\/en/, '') || '/';
}

const displayText = locale === 'ja' ? 'EN' : 'JA';
---

<a
  href={alternateUrl}
  class="inline-flex items-center gap-1 px-3 py-1.5 text-sm font-medium text-warm-gray-700 hover:text-primary-600 border border-warm-gray-200 hover:border-primary-500 rounded-soft transition-colors"
  aria-label={locale === 'ja' ? 'Switch to English' : '日本語に切り替える'}
>
  <svg
    class="w-4 h-4"
    fill="none"
    stroke="currentColor"
    viewBox="0 0 24 24"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129"
    ></path>
  </svg>
  {displayText}
</a>
